const React = require('react');
const { useState, useRef, Component } = React;
import Tr from './Tr';

const Table = () => {

    return (
            <table>
                <Tr>{''}</Tr>
            </table>
        )
}

export default Table;